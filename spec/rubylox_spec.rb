# frozen_string_literal: true

RSpec.describe Rubylox do
  it "has a version number" do
    expect(Rubylox::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
